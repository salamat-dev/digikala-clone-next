/* شکل رکورد در mockapi — نام فیلدها خط تیره دارد */
export interface User {
  id: string;
  fname: string;
  lname: string;
  phoneNum: string;
  email: string;
  addres: string;
  age: number;
  pass: string;
}